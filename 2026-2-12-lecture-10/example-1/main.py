from io_util import print_factorial_equation, number_input
from my_funcs import calculate_factorial 

num = number_input("Enter a number to get the factioral of: ")
print_factorial_equation(num)
print(calculate_factorial(num))
